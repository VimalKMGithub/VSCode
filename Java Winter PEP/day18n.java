public class day18n {
    // "SELECT FirstName, LastName FROM Employees WHERE DepartmentID = 101"

    // CREATE TABLE Student (StudentID INT PRIMARY KEY,StudentName VARCHAR(255));
    // CREATE TABLE Course (CourseID INT PRIMARY KEY, CourseName VARCHAR(255));
    // ALTER TABLE Student ADD COLUMN CourseID INT,ADD FOREIGN KEY (CourseID)
    // REFERENCES Course(CourseID);
}
// Which type of database is sql ?
// options
// 1. RDBMS [correct answer is 1]
// 2. DBMS

// In SQL, what is the purpose of the JOIN keyword?
// options
// 1: To insert new records into a table
// 2: To update existing records in a table
// 3: To retrieve data from multiple tables [correct answer is 3]
// 4: To delete records from a table